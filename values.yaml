# Default values for the User Service Helm chart

replicaCount: 1
image:
  repository: ghcr.io/nooshet-backend/user-service
  tag: development-c592859
  pullPolicy: IfNotPresent
  imagePullSecrets:
    - name: ghcr-secret
service:
  type: ClusterIP
  port: 80
ingress:
  enabled: true
  annotations: {}
  hosts:
    - host: user.nooshet.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
resources: {}
autossl: {}
