# Default values for the User Service Helm chart

replicaCount: 1
image:
  repository: nooshet/user-service
  tag: development-ae7a45f
  pullPolicy: IfNotPresent
service:
  type: ClusterIP
  port: 80
ingress:
  enabled: true
  annotations: {}
  hosts:
    - host: user.nooshet.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
resources: {}
autossl: {}

istio:
  enabled: false
  authorization:
    internalCallers: []
